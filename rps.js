const randomNum = Math.floor((Math.random() * 3) + 1);
const rock = "Rock!";
const paper = "Paper!";
const scissors = "Scissors!";
const playerInput = prompt("Rock, Paper, Scissors, Shoot! Type in your choice below");


console.log(randomNum); 

function computerPlay() {
    if (randomNum === 1) {
      return rock;
    } else if (randomNum === 2) {
        return paper;
    } else {
        return scissors;
    }
}

function playRound (computerSelection, playerSelection) {
    playerInput;
    computerPlay()
    if (playerInput === 

}
  